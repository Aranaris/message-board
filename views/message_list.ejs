<header><%= title  %></header>
<% for( let index = 0; index < message_list.length; index++ ) { %>
  <span>"<%= message_list[index].message_text %>" - <%= message_list[index].user.username %> (Posted <%= message_list[index].post_date %>)<br /></span>
<% } %>
