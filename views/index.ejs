<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <% for( let index = 0; index < messages.length; index++ ) { %>
      <span><%= messages[index].text %> - <%= messages[index].user %> (Posted <%= messages[index].added %>)<br /></span>
    <% } %>
  </body>
</html>
